# Produce a tuple
[for x in var.<variable_id> : <actions>]

# Produce an object
{for x in var.<variable_id> : <actions>}

# Map or transform elements from a collection into a new form
{for x in var.<variable_id> : x => <actions>}
